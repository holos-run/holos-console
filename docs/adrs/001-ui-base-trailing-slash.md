# ADR 001: Canonicalize UI base path without trailing slash

## Status

Superseded — The UI now serves at `/` using TanStack Router (no basename needed). Legacy `/ui` and `/ui/` paths redirect to `/` for backwards compatibility. This ADR is retained for historical context only.
