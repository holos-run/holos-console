# ServiceAccount the holos-console pod runs as. ADR 036 binds human
# impersonation and RBAC reconciliation privileges to this single pod-level
# identity; later deployment manifests must use this name explicitly.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: holos-console
  namespace: holos-system
